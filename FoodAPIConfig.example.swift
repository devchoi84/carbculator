//
//  FoodAPIConfig.example.swift  (템플릿)
//
//  실제 키가 담긴 carbculator/FoodAPIConfig.swift 는 .gitignore 되어 원격에 올라가지 않습니다.
//  새 환경에서 빌드하려면 이 템플릿을 참고해 아래 경로에 파일을 만드세요:
//
//      carbculator/FoodAPIConfig.swift
//
//  serviceKeyEncoded 에는 공공데이터포털에서 발급한 "인코딩(Encoding)" 서비스키
//  (%2B, %3D 등이 포함된 값)를 그대로 넣습니다.
//
//  (이 .example 파일은 앱 타깃 폴더 밖에 있어 컴파일되지 않습니다.)

import Foundation

enum FoodAPIConfig {
    static let serviceKeyEncoded = "YOUR_ENCODED_SERVICE_KEY_HERE"
}
